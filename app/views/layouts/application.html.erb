<!DOCTYPE html>
<html>
  <head>
    <title>EbookPlatform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-zinc-900">
    <div class="flex w-full items-center justify-around p-8">
      <div>
        <h1 class="text-3xl font-semibold text-white"><span class="text-green-400">eBook</span>Platform</h1>
      </div>
      <div>
        <ul class="flex z-50">
            <% if user_signed_in? %>
              <%= link_to 'Sair', destroy_user_session_path, method: :delete, class: "p-2 text-white font-semibold bg-green-400 rounded-lg ml-5 cursor-pointer" %>
            <% else %>
              <%= link_to 'Entrar', new_user_session_path, class: "p-2 text-white cursor-pointer" %>
              <%= link_to 'Cadastre-se', new_user_registration_path, class: "p-2 text-white font-semibold bg-green-400 rounded-lg ml-5 cursor-pointer" %>
            <% end %>
        </ul>
      </div>
    </div>
    <%= yield %>
    <div class="flex w-full items-center justify-around p-8 bg-zinc-950 mt-40 h-[250px]">
      <h1 class="text-3xl font-semibold text-white"><span class="text-green-400">eBook</span>Platform</h1>
      <p class="text-base text-white">@todosdireitosreservados</p>
      <p class="text-base text-white">eBookPlatform@contato.com</p>
    </div>
  </body>
</html>
